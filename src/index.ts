/**
 * web-app-bff — entry point stub
 *
 * WEB-BFF-001 Phase US-2 (Jessie): This file is a compile-gate stub ONLY.
 * Blake implements startServer(), shutdown(), createApp(), and all routes in Phase US-3.
 *
 * Test Lock Rule (CLAUDE.md §6): Blake MUST NOT modify test files.
 * Blake MUST replace this entire stub with the real implementation.
 */

// Compile-gate stub — exports nothing until Blake implements (Phase US-3).
// This satisfies tsc TS18003 "no inputs" error so `npm run build` exits 0.
export {};
